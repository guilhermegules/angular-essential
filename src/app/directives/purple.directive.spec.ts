import { PurpleDirective } from './purple.directive';

describe('PurpleDirective', () => {
  it('should create an instance', () => {
    // const directive = new PurpleDirective();
    // expect(directive).toBeTruthy();
  });
});
